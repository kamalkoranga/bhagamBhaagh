[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "bcrypt>=5.0.0",
    "fastapi[standard]>=0.141.1",
    "python-jose>=3.5.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
